<style>

  .content {
    width: 75%;
    background: rgba(255,255,255,0.75);
    background-image: 
  		linear-gradient(0deg, rgba(0,0,0,0), rgba(0,0,0,0.1) 50%, rgba(0,0,0,0)), 
  		linear-gradient(0deg, rgba(255,255,255,0), rgba(255,255,255,0.8) 50%, rgba(255,255,255,0)), 
  		radial-gradient(50% 100%, ellipse cover, rgba(0,0,0, 0.05), rgba(0,0,0,0) 50%);
    padding: 20px;
    overflow: hidden;
    position: center;
	margin: 0 auto; /* Отступ слева и справа */
}

.content img {
    margin-right: 15px;
    float: left;
}


</style>
<div class="content">
	
		<% if current_user.userparam != nil %>
		<p><%= image_tag current_user.userparam.avatar.url(:thumb), size: '400x400', class: 'img-rounded'%> </p>
	
		<span class="btn btn-info"> <%= link_to "Edit info", userparams_edit_path(current_user)%>  </span>
	 	<% else %>
		<span class="btn btn-info"> <%= link_to "Add info", userparams_new_path %>  </span>
		<%end%>
		<span class="btn btn-info"> <%= link_to "Back", root_path %>  </span>
	
</div>